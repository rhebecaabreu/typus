<%
  @page_title = t('.title')
  admin_title(@page_title)
%>

<%= form_for Typus.class.name.to_sym,
             url: { action: :create },
             html: { class: "form-signin" } do |form| %>
  <%= render partial: "form", locals: { form: form } %>
<% end %>
